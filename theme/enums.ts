export enum Token {
  COLOUR = 'colour',
  FONT = 'font',
  SPACING = 'spacing'
}

export enum Colour {
  NEUTRAL = 'neutral',
  BLUE = 'blue',
  CYAN = 'cyan'
}

export enum Unit {
  PX = 'px',
  EM = 'em',
  REM = 'rem'
}
